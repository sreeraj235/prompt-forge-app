{
  "name": "prompt-forge",
  "version": "1.0.0",
  "description": "Backend + frontend for Prompt Forge \u2014 analyzes an input and generates a ready-to-use prompt.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express-rate-limit": "^7.4.0"
  }
}
